import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';

class MapHWidget extends StatelessWidget {

  MapHWidget();
  
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("map"),
    );
  }
}